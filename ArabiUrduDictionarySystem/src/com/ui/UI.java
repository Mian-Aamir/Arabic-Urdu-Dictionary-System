package com.ui;

/**
 * Common User Interface contract for all front-ends (CLI / GUI).
 * Every UI must implement a start() method to begin user interaction.
 */
public interface UI {
    void start();
}
